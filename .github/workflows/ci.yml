# gitops-app/.github/workflows/ci.yml
name: CI Pipeline

on:
  push:
    branches: [main]
    paths:
      - 'ops-app/**'
      - 'github/workflows/ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'ops-app/**'
      
env:
  DOCKERHUB_IMAGE: zionn1002/gitops-app
  CONFIG_REPO: zionn1002/gitops-config
  CONFIG_REPO_PATH: apps/gitops-app/overlays

permissions:
  contents: read

jobs:
  ci:
    name: Build and Push
    runs-on: ubuntu-24.04
    outputs:
      image-tag: ${{ steps.tag.outputs.tag }}

    steps:
      - uses: actions/checkout@v4

      - name: Java 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: 테스트 실행
        working-directory: ops-app
        run: mvn test -q

      - name: 이미지 태그 생성
        id: tag
        run: echo "tag=sha-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: DockerHub 로그인
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker 빌드 및 Push
        working-directory: ops-app
        env:
          IMAGE_TAG: ${{ steps.tag.outputs.tag }}
        run: |
          docker build -t $DOCKERHUB_IMAGE:$IMAGE_TAG .
          docker push $DOCKERHUB_IMAGE:$IMAGE_TAG
          docker tag $DOCKERHUB_IMAGE:$IMAGE_TAG $DOCKERHUB_IMAGE:latest
          docker push $DOCKERHUB_IMAGE:latest

  # update-config:
  #   name: Update Config Repo
  #   needs: ci
  #   runs-on: ubuntu-24.04
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  #   steps:
  #     - name: Config Repo 체크아웃
  #       uses: actions/checkout@v4
  #       with:
  #         repository: ${{ env.CONFIG_REPO }}
  #         token: ${{ secrets.CONFIG_REPO_PAT }}
  #         path: config-repo

  #     - name: Kustomize 설치
  #       run: |
  #         curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
  #         sudo mv kustomize /usr/local/bin/

  #     - name: dev 이미지 태그 업데이트
  #       env:
  #         IMAGE_TAG: ${{ needs.ci.outputs.image-tag }}
  #       run: |
  #         cd config-repo/${{ env.CONFIG_REPO_PATH }}/dev
  #         kustomize edit set image \
  #           nginx=your-dockerhub-username/gitops-app:$IMAGE_TAG

  #     - name: Config Repo 커밋 및 Push
  #       env:
  #         IMAGE_TAG: ${{ needs.ci.outputs.image-tag }}
  #       run: |
  #         cd config-repo
  #         git config user.name "github-actions[bot]"
  #         git config user.email "github-actions[bot]@users.noreply.github.com"
  #         git add .
  #         git diff --staged --quiet || git commit \
  #           -m "chore: update dev image to $IMAGE_TAG [skip ci]"
  #         git push
